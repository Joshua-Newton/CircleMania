Known bugs in the game:

1.  Player moves faster diagonally. NOTE: can fix this in graphicsTimers.h where movement is handled

2.  When first shooting, the target position (The current mouse position) sometimes does not register correctly. This
    is fixed after moving the mouse, but returns if the mouse button is released, moved to another position, then clicked
    without moving the mouse.

3.  Hover over buttons activates when mouse is not over rectangles directly after changing screens. This fixes as soon
    as you move the mouse
